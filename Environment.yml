name: OTF
channels:
  - conda-forge
dependencies:
  - jupyter 
  - pip >= 24.0
  - python = 3.12
  - pip:
      - numpy
      - pandas
      - matplotlib
      - seaborn
      - scikit-learn
      - scipy
      - statsmodels
      - stable-baselines3
      - gymnasium
      - tensorboard
      - tensorflow